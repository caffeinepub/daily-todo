{
  "kind": "build_request",
  "title": "Add dark mode toggle to reduce eye strain",
  "priority": "normal",
  "requirements": [
    {
      "id": "REQ-11",
      "text": "Add a dark mode toggle button in the application header that allows users to switch between light and dark color schemes",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-user-dark-mode"
        ],
        "quotes": [
          "looks good, my eyes are burning can you add a dark mode?"
        ]
      },
      "acceptanceCriteria": [
        "A toggle button is visible in the application header",
        "Clicking the toggle switches between light and dark modes",
        "The selected mode persists across page refreshes",
        "The toggle button icon or label clearly indicates the current mode"
      ]
    },
    {
      "id": "REQ-12",
      "text": "Implement dark mode styling by applying the 'dark' class to the html or body element and ensuring all components respect the existing dark mode OKLCH color variables defined in index.css",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-user-dark-mode"
        ],
        "quotes": [
          "looks good, my eyes are burning can you add a dark mode?"
        ]
      },
      "acceptanceCriteria": [
        "Dark mode uses the warm earth-toned color palette already defined in the CSS variables",
        "All text remains readable with appropriate contrast in dark mode",
        "Background colors, borders, and shadows adapt to dark mode",
        "The PassiveAggressiveModal dialog styling works correctly in both modes",
        "Task checkboxes, edit buttons, and input fields are properly styled in dark mode"
      ]
    },
    {
      "id": "REQ-13",
      "text": "Store the user's dark mode preference in localStorage so it persists across browser sessions and is applied immediately on page load",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-user-dark-mode"
        ]
      },
      "acceptanceCriteria": [
        "The dark mode preference is saved to localStorage when toggled",
        "On page load, the saved preference is read and applied before rendering",
        "If no preference exists, default to light mode",
        "No flash of unstyled content occurs when loading in dark mode"
      ]
    }
  ],
  "constraints": [
    "Use the existing OKLCH color variables already defined in frontend/index.css for dark mode",
    "Do not modify the warm earth-toned color palette",
    "Do not change backend files or data models"
  ],
  "nonGoals": [
    "Creating additional color themes beyond light and dark",
    "Adding system preference detection for automatic dark mode",
    "Modifying the PassiveAggressiveModal content or behavior"
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  }
}